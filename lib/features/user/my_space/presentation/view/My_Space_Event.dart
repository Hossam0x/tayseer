import 'package:tayseer/my_import.dart';

class MySpaceEventContent extends StatelessWidget {
  const MySpaceEventContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('محتوى مساحتى - الاحداث'));
  }
}
